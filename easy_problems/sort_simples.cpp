#include <iostream>
#include <vector>

using namespace std;

int main(){
	int a, b, c;
	cin a, b, c;

	vector<int> values;
	values.push_back(a);
	values.push_back(b);
	values.push_back(c);

	
	return 0;
}
